# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.project
.classpath
.settings/


# OS
.DS_Store
Thumbs.db

# Agent harness / local working notes — none of this is part of the extension.
.claude
.planning
.superpowers
scripts
Flowjo.md
Claude.md
docs/superpowers
notebooks

# Git worktrees created by the harness. Was only in .git/info/exclude, which is
# local to one clone and never shared — so a fresh clone tracked them.
.worktrees/

# Sample / input data. cells.geojson from MIRAGE is ~150MB; never commit one.
*.geojson
# MkDocs build output
site/
